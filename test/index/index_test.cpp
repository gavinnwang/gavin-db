

#include "gtest/gtest.h"
namespace db {

TEST(IndexTest, IndexTest) {
	// BTreeContainer<Value, Value, std::less<Value>> container;
	// Index<BTreeContainer<Value, Value, std::less<Value>>> index;
	// auto t = Tuple();
	// auto r = RID();
	// index.InsertRecord(t, r);

	// std::vector<Index> indexes;
}
} // namespace db
